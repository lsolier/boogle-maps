spring:
  application:
    name: boogle-maps

#Eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    service-url:
      default-zone: http://localhost:8761/eureka/
instance:
  preferIpAddress: true

server:
  port: 9003

